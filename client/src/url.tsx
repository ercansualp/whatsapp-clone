export const serverPort = "5000"
export const serverIP = "localhost"
export const userAPI = `http://${serverIP}:${serverPort}/user`
export const messageAPI = `http://${serverIP}:${serverPort}/message`